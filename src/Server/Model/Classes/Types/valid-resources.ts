export enum ValidResources {
  name = 'name',
  entities = 'entities',
  attributes = 'attributes',
  associations = 'associations',
}

export enum ValidProperties {
  name = 'name',
  type = 'type',
  source = 'source',
  target = 'target',
}
